.worktrees/
node_modules/

# Claude Code local state: per-machine permissions, and whole checkouts —
# a `git add -A` would otherwise commit a nested worktree.
.claude/settings.local.json
.claude/worktrees/

# harness verify scratch under fixtures
test/fixtures/**/.team-up-verify-run-*/

# runtime log, never belongs in the repo
.tim/
